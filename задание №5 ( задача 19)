#include <cmath>
#include <iostream>

double RingS(double r1, double r2) {
  return 3.14 * (r1 * r1 - r2 * r2);
}

int main() {
  for (size_t i = 0; i < 3; ++i) {
    double r1, r2;
    std::cin >> r1 >> r2;
    std::cout << RingS(r1, r2) << ' ';
  }
  return 0;
}
